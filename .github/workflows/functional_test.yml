name: Functional Tests

# Manual trigger only — run from the Actions tab.
# Provide a token and optionally a base URL and root token.

on:
  workflow_dispatch:
    inputs:
      token:
        description: "Database API token (required)"
        required: true
        type: string

      base_url:
        description: "API base URL — e.g. http://your-server:8050/api/v2. Leave blank to use the default Endee endpoint."
        required: false
        default: ""
        type: string

      root_token:
        description: "Root token (NDD_ROOT_TOKEN) — optional. When provided, admin tests are included."
        required: false
        default: ""
        type: string

jobs:
  functional-test:
    name: Endee Functional Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-timeout numpy

      - name: Verify server connectivity
        env:
          ENDEE_TOKEN:    ${{ inputs.token }}
          ENDEE_BASE_URL: ${{ inputs.base_url }}
        run: |
          python - <<'PYEOF'
          import os, sys
          from endee import Endee

          token    = os.environ.get("ENDEE_TOKEN")    or None
          base_url = os.environ.get("ENDEE_BASE_URL") or None

          if not token:
              print("ENDEE_TOKEN is not set", file=sys.stderr)
              sys.exit(1)

          c = Endee(token=token)
          if base_url:
              c.set_base_url(base_url)

          try:
              health = c.health()
              cols   = c.list_collections()
              print(f"Server is up (status={health.get('status')}). Existing collections: {len(cols)}")
          except Exception as e:
              print(f"Connection failed: {e}", file=sys.stderr)
              sys.exit(1)
          PYEOF

      - name: Run functional tests
        env:
          ENDEE_TOKEN:    ${{ inputs.token }}
          ENDEE_BASE_URL: ${{ inputs.base_url }}
          NDD_ROOT_TOKEN: ${{ inputs.root_token }}
        run: |
          pytest tests/ \
            -v \
            --timeout=120 \
            --tb=short \
            -p no:warnings \
            --junit-xml=test-results.xml

      - name: Write job summary
        if: always()
        run: |
          python - <<'PYEOF'
          import xml.etree.ElementTree as ET, os, sys

          summary_file = os.environ.get("GITHUB_STEP_SUMMARY", "")
          if not summary_file:
              sys.exit(0)

          def write(content):
              with open(summary_file, "a") as f:
                  f.write(content)

          try:
              tree  = ET.parse("test-results.xml")
              root  = tree.getroot()
              suite = root if root.tag == "testsuite" else root.find("testsuite")
              if suite is None:
                  suite = root

              total    = int(suite.get("tests",    0))
              failures = int(suite.get("failures", 0))
              errors   = int(suite.get("errors",   0))
              skipped  = int(suite.get("skipped",  0))
              passed   = total - failures - errors - skipped
              elapsed  = float(suite.get("time",   0))
              mins, secs = divmod(int(elapsed), 60)
              duration = f"{mins}m {secs}s" if mins else f"{secs}s"

              icon = "✅" if (failures + errors) == 0 else "❌"

              lines = [
                  f"## {icon} Endee Functional Test Results",
                  "",
                  "| Result   | Count |",
                  "|----------|------:|",
                  f"| Passed   | {passed} |",
                  f"| Failed   | {failures} |",
                  f"| Errors   | {errors} |",
                  f"| Skipped  | {skipped} |",
                  f"| **Total** | **{total}** |",
                  "",
                  f"**Duration:** {duration}",
                  "",
              ]

              failed_cases  = []
              skipped_cases = []
              for tc in suite.iter("testcase"):
                  for tag in ("failure", "error"):
                      node = tc.find(tag)
                      if node is not None:
                          name = tc.get("name", "unknown")
                          cls  = tc.get("classname", "")
                          msg  = (node.get("message") or node.text or "").strip().split("\n")[0][:200]
                          failed_cases.append((cls, name, msg))
                  skip_node = tc.find("skipped")
                  if skip_node is not None:
                      name   = tc.get("name", "unknown")
                      cls    = tc.get("classname", "")
                      reason = (skip_node.get("message") or "No reason given").strip()
                      skipped_cases.append((cls, name, reason))

              if failed_cases:
                  lines += [
                      "<details>",
                      f"<summary>Failed tests ({len(failed_cases)})</summary>",
                      "",
                      "| Test | Reason |",
                      "|------|--------|",
                  ]
                  for cls, name, msg in failed_cases:
                      lines.append(f"| `{cls}::{name}` | {msg} |")
                  lines += ["", "</details>", ""]

              if skipped_cases:
                  from collections import defaultdict
                  by_reason = defaultdict(list)
                  for cls, name, reason in skipped_cases:
                      by_reason[reason].append(f"`{cls}::{name}`")

                  lines += [
                      "<details>",
                      f"<summary>Skipped tests ({len(skipped_cases)})</summary>",
                      "",
                  ]
                  for reason, names in by_reason.items():
                      lines.append(f"**Reason:** {reason}")
                      lines.append("")
                      for n in names:
                          lines.append(f"- {n}")
                      lines.append("")
                  lines += ["</details>", ""]

              write("\n".join(lines) + "\n")
              print(f"Summary written: {passed}/{total} passed in {duration}")

          except FileNotFoundError:
              write("## Test Report\n\n`test-results.xml` not found — pytest may not have run.\n")
          except Exception as exc:
              print(f"Summary generation failed: {exc}", file=sys.stderr)
          PYEOF

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: endee-functional-test-results
          path: test-results.xml
          retention-days: 7
