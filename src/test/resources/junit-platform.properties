# Enables ServiceLoader-based auto-registration of globally-applied extensions,
# specifically support.TestProgressWatcher (see META-INF/services). No test behavior
# is affected - this only turns on a reporting listener.
junit.jupiter.extensions.autodetection.enabled=true
